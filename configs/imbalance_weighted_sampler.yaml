# Class-imbalance ablation C: weighted random sampling (oversample rare classes)
# Compare against imbalance_none.yaml and imbalance_weighted_loss.yaml
model:
  name: resnet50
  pretrained: true
train:
  epochs: 60
  best_metric: balanced_accuracy
  class_weight: none
  sampler: weighted
data:
  aug: randaug
