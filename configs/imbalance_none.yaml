# Example training config: class-imbalance ablation (no rebalancing)
# Compare against configs/imbalance_weighted.yaml
model:
  name: resnet50
  pretrained: true
train:
  epochs: 60
  best_metric: balanced_accuracy
  # class_weight: none   (placeholder for the imbalance-ablation experiment)
data:
  aug: randaug
